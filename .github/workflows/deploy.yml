# name: Deploy to Vercel

# on:
#   push:
#     branches: [main]

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Setup PHP
#         uses: shivammathur/setup-php@v2
#         with:
#           php-version: '8.2'

#       - name: Install Composer dependencies
#         run: composer install --no-dev --optimize-autoloader

#       - name: Install Node dependencies
#         run: npm install

#       - name: Build assets
#         run: npm run assets || npm run assets

#       - name: Build site
#         run: npm run build

#       - name: Install Vercel CLI
#         run: npm install -g vercel@latest

#       - name: Create Vercel config
#         run: |
#           mkdir -p build_production/.vercel
#           echo '{"projectId":"${{ secrets.VERCEL_PROJECT_ID }}","orgId":"${{ secrets.VERCEL_ORG_ID }}"}' > build_production/.vercel/project.json
#           echo '{"buildCommand":"","outputDirectory":"."}' > build_production/vercel.json

#       - name: Deploy to Vercel
#         run: vercel deploy --prod --token=${{ secrets.VERCEL_TOKEN }} --yes
#         working-directory: ./build_production
